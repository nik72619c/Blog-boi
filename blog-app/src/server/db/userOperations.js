const userOperations= {
    loginOrRegister: async ()=>{
        //TODO create a function API for adding the user to the database
    },
    logoutUser: async ()=>{
    //TODO logout the current user
    }
}

module.exports=userOperations;